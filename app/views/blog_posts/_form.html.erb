<%= simple_form_for(@blog_post) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.input :blog_image %>
  <% if current_user.master %>
    <%= f.input :published, as: :boolean, label: 'Publish in newsfeed' %>
  <% end %>
  <%= f.submit %>
<% end %>
